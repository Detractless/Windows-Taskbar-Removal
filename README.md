# Windows-Taskbar-Removal
WindowsTaskbarRemoval — Hides the Windows Explorer taskbar at logon for all users using the AppBar API, with automatic recovery on Explorer restart and clean restore on exit.
